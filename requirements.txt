# SafetyLM — Python dependencies (LOCKED MIRROR — generated, do not edit by hand)
#
# Source of truth: pyproject.toml + uv.lock (managed with `uv`).
# Regenerate this file:  uv export --no-hashes --no-dev > requirements.txt
# Install without uv:    pip install -r requirements.txt
#
# All deps are permissively licensed (MIT/BSD/Apache) for Apache-2.0 compatibility.
# (Phase 2+ deps — llama-index, qdrant-client, ragas, chainlit — are added when reached.)
anyio==4.14.1
beautifulsoup4==4.15.0
certifi==2026.6.17
cffi==2.0.0 ; platform_python_implementation != 'PyPy'
charset-normalizer==3.4.7
cryptography==49.0.0
et-xmlfile==2.0.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
lxml==6.1.1
openpyxl==3.1.5
pdfminer-six==20260107
pdfplumber==0.11.10
pillow==12.2.0
pycparser==3.0 ; implementation_name != 'PyPy' and platform_python_implementation != 'PyPy'
pypdfium2==5.11.0
regex==2026.6.28
requests==2.34.2
soupsieve==2.8.4
tiktoken==0.13.0
typing-extensions==4.15.0
urllib3==2.7.0
